export class Point {

    horizontalCoord: number;
    verticalCoord: number;

    constructor(horizontalCoord: number, verticalCoord: number) {
        this.horizontalCoord = horizontalCoord;
        this.verticalCoord = verticalCoord;
    }
}
